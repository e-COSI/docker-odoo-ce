RUN set -x; \
        apt-get update \
        && apt-get upgrade -y \
        && apt-get install -y --no-install-recommends \
            ca-certificates \
            curl \
      	    git \
      	    npm \
      	    build-essential \
      	    python-dev \
      	    python-pip \
            node-less \
            node-clean-css \
         	  nodejs \
      	    nodejs-legacy \
      	    cython \
      	    libldap2-dev \
      	    libsasl2-dev \
      	    libxml2-dev \
      	    zlib1g-dev \
      	    libxslt1-dev \
            libjpeg-dev